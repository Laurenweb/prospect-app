<form method="post" action="{{ path('ambassador_delete', {'id': user.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer cet article?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
