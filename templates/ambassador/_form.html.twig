{{ form_start(form) }}
    {{ form_row(form.email) }}
    {{ form_row(form.plainPassword, {
		label: 'Mot de passe'
	}) }}
    {{ form_row(form.isVerified, {
		label: 'Compte vérifié'
	}) }}
    <button class="btn btn-info mb-3">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
